# iSS PowerAI v1.4.1 - Ollama Thinking UI

- Hiển thị `Đang suy nghĩ` với biểu tượng robot và ba chấm nhấp nháy trong thời gian chờ.
- Không hiển thị reasoning/thinking của model.
- Backend và frontend cùng lọc thẻ `<think>` để tránh rò rỉ nội dung suy luận.
- Chỉ hiển thị kết quả cuối cùng.
